# 2026-07-28 工作记录

## 修复直播源窗口（ShowReplaceUrlDialog）分析与现代 UI 设计
- 现状事实（已读 L7202-7373）：固定 480×320 对话框，绝对像素布局（Location/Size），标题"将可用IP+端口或网址替换为新地址"；字段：原始地址(只读)、新IP/域名、新端口、预览(只读，由 protocol+host+port+path 拼出)；按钮 确定/取消；逻辑 AutoParseIpPort 拆分 ip:port、UpdatePreview 实时拼接、OK 时改 ch.Url 并重置状态。兄弟窗口 ReplaceAllUrls（一键全部替换）在 L7375+。
- 设计问题（判断/建议，非事实）：①绝对坐标布局、字号偏小(7.5f)；②原始地址与预览两个只读框冗余；③仅 OK 时才校验"IP/域名不能为空"，无实时校验；④按钮无主次（两个 outline）；⑤无图标/分组/语法高亮；⑥标题是一句话不够凝练。
- 产出：生成 `修复直播源窗口_现代设计.html`，含现状分析 + 3 款可视化设计（A 玻璃拟态居中卡 / B Fluent 分栏+语法高亮 / C 行内编辑+变更差异高亮），用户待选。沙箱 Linux 仅出 HTML 稿，落地为 WinForms 需本机实现。
- 注：本任务为设计稿，未改动 C# 代码。待用户选定一款后再实现。

## 执行方案 B（Fluent 分栏高亮）落地为 WinForms
- 用户选择 B 并要求"严格按展示效果 + 自动适配主题"。已重写 `ShowReplaceUrlDialog`（L7186 起）主体：
  - 左侧 4px 强调条（Panel，`theme.Accent`，圆角 Region）。
  - 小号灰标签（`theme.TextSecondary`，`GetFont(SF(9f))`），标题改为系统 caption "修复直播源地址"。
  - 输入框统一用 `StyleRoundTextBox` 包 Panel 圆角（原始地址 `theme.BgAlt`、IP/端口 `theme.Surface`，边框 `theme.Border`）。
  - **结果框语法高亮**：`RichTextBox`（只读、`DetectUrls=false`、无滚动条、Consolas SF(11)），背景 `LerpColorStatic(theme.Surface, accent, 0.14)`（accent 浅色）、边框 `LerpColorStatic(theme.Surface, accent, 0.35)`；`UpdatePreview` 分段着色：protocol/path 用 `theme.TextSecondary`，host:port 用 `ControlPaint.Light(accent,0.4f)` 加粗。
  - 按钮：取消=Outline(`StyleOutlineButton`)；确定替换=实心强调(新增 `StylePrimaryButton` 静态方法，圆角填充 accent + hover 提亮，白字)。
  - 所有颜色均由 `theme` 派生（accent 浅色/边框用 LerpColorStatic 混合），实现主题自动适配。
- 新增辅助方法 `StylePrimaryButton(Button,int,Color?)`（位于 `StyleOutlineButton` 之后，复用 `GetRoundedPath`/`LerpColorStatic`/`ControlPaint`）。
- 校验：`dotnet build -p:OutputPath=bin/Verify/` 0 警告 0 错误。**GUI 运行时仍需本机 Windows 验证**（沙箱 Linux）。
- 设计稿 HTML 保留于项目根 `修复直播源窗口_现代设计.html`。

## 撤销方案 B（回退到原始对话框）
- 用户要求"恢复B方案代码"，经澄清为"撤销方案 B、恢复改造前原始对话框"。
- 已回退 `ShowReplaceUrlDialog`：标题恢复"直播源修复"；主体恢复原始（标题"将可用IP+端口或网址替换为新地址"、绝对像素布局、原始地址/预览两只读框、按钮 确定/取消 均为 Outline、UpdatePreview 写 `txtPreview.Text`）；删除了方案 B 专属的 `accentBar`/`RichTextBox rtbResult`/`AddLabel`/`AddInput` 辅助方法与语法高亮逻辑。
- 删除了仅为方案 B 新增的静态方法 `StylePrimaryButton`（无其他引用）。
- 校验：`dotnet build -p:OutputPath=bin/Verify/` 0 警告 0 错误；grep 确认无 `StylePrimaryButton`/`accentBar`/`rtbResult`/`AddInput` 残留引用。
- 现状：修复直播源窗口已回到方案 B 之前的原始形态。设计稿 HTML 仍保留供将来参考。

## 主窗口主题设计（apple-design，可视化稿）
- 任务：为"主窗口"设计几款好看主题并可视化展示，供用户选择后接入。加载 apple-design 技能。
- 已读 `AppTheme.cs`：现有 6 款静态主题（暖玉米白/青瓷薄荷/淡雾蓝/藕粉柔光/深空墨蓝/墨绿夜）+ `Light`(=MintCeladon)/`Dark`(=DeepInk) 别名 + `GetAutoTheme` + `ApplyHighContrast`；字段 Bg/BgAlt/Surface/Border/TextPrimary/Secondary/Accent/Primary/NavBg/HeaderBg/SelectRow/StatusBarBg/PlayBtnBg/CopyBtnBg/LinkTextColor/Success/Error/Warn/Info 等。
- 已确认主题切换机制：标题栏 `btnThemeToggle`(三角)→`themeMenu` ContextMenuStrip，逐项 `SetTheme(AppTheme.Xxx)`。新增主题=AppTheme.cs 加静态实例 + themeMenu 加一项，现有 `ApplyTheme`/`ApplyThemeToControl` 体系自动应用，无需改布局。
- 已设计 4 款彼此区分且"有苹果味"的主题（克制中性色 + 单一强调色 + elevation 靠毛玻璃/细描边/投影 + 切换 350ms 缓动）：
  1. 石墨灰 Graphite（深色·中性灰无蓝调，Accent #2997ff）——区别于现有"深空墨蓝"
  2. 晴空蓝 Daylight（浅色·Apple 系统蓝 #0071e3，近白）
  3. 绛紫 Royal Violet（深色·高级紫 #bf5af2）
  4. 暖砂 Sunset Sand（浅色·暖琥珀 #e8820c 配米白）——区别于现有"暖玉米白"
- 产出：`主窗口主题_可视化设计.html`，含可交互主页预览（标题栏/工具栏/侧边栏/数据网格/预览面板/状态栏，颜色严格映射到 AppTheme 字段）+ 调色板实时跟随 + 接入说明。本任务为设计稿，**未改 C# 代码**，待用户选定后落地。
- 注：沙箱 Linux 只能出 HTML 稿与编译验证，GUI 视觉需本机 Windows 确认。

## 主窗口动态主题设计（apple-design，可视化稿 · 第二版）
- 用户再次要求"设计几款动态主题"。第一版（流光极光/脉动珊瑚/呼吸深海/流体玻璃）偏"流光/呼吸"单一维度；本版升级为更明确的"动态"维度：背景流动 + **随系统时间自动渐变** + **强调色沿色相环流动** + 毛玻璃。
- 已重新生成 `主窗口动态主题_可视化设计.html`（同名覆盖第一版），含 4 款带真实 CSS 动效的主窗口 Mockup（标题栏/工具栏/侧边栏/数据网格/预览/状态栏，颜色映射到 AppTheme 字段）：
  1. **极光流光 Aurora**（深色）：整窗背景 linear-gradient 漂移(19s) + 预览窗 conic-gradient 旋转光晕 + 选中行 box-shadow 呼吸(3.2s)。Accent #38e1c6 薄荷绿。
  2. **昼夜律动 DayCycle**（时间感知）：JS 每 3.6s 在 晨/午/昏/夜 四套调色板间切换，CSS @property + 1.5s 过渡做平滑渐变（真实使用按 `DateTime.Now.Hour` 整点切换）。
  3. **霓虹脉冲 Neon Pulse**（深色）：强调色元素(主按钮/选中行名/侧栏选中/状态栏/预览)挂 `hue-rotate` 13s 循环，沿色相环流动。Accent 品红 #ff2d95 起点。
  4. **流体玻璃 Liquid Glass**（浅色）：面板 `backdrop-filter: blur` 毛玻璃 + 光泽层缓慢游移(12s)。Accent #5e5ce6 靛蓝。
- 落地要点（文档已写，关键性能边界）：WinForms 用 `Timer` 低频驱动；静态控件只在相位变化时整体刷新一次；连续动效只画单一背景层(自定义 OnPaint 双缓冲 Panel)，不与数据网格耦合；呼吸/脉冲只 Invalidate 少数控件。绝不可每 16ms 遍历几百行网格重设 BackColor（会闪烁/卡顿）。
- 本任务为设计稿，**未改 C# 代码**，待用户选定后落地。沙箱 Linux 只能出 HTML 演示，动效需在浏览器查看；GUI 落地需本机 Windows 实现与验证。若用户更想要第一版的"纯流光/呼吸"风格，可再生成。

## 主窗口动态动效主题（第三版 · 聚焦"动效语言"，apple-design）
- 用户再次要求"设计几款动态动效主题"。与前两版（侧重配色/背景流动/时间律动）不同，本版聚焦**动效语言本身**，用真实 CSS 动画演示，遵循 apple-design（响应按压、克制、reduced-motion 降级）。
- 新增文件 `主窗口动态动效主题_可视化设计.html`，含可交互主窗口 Mockup（标题栏/工具栏/侧边栏/网格/预览/状态栏），4 款各具标志性动效：
  1. **极光流光 Aurora**（深色）：标题栏底部多色极光带流动(9s linear) + 选中行辉光呼吸(4.5s)。
  2. **呼吸脉冲 Breath**（浅色）：状态圆点呼吸(2.4s) + 行入场淡入上移(rowIn) + 浅色安静。
  3. **液态玻璃 Liquid Glass**（浅色）：毛玻璃标题栏(backdrop-filter) + 按钮 hover 液态扫光(shimmer) + 按压缩放到 .94。
  4. **霓虹脉动 Neon Pulse**（深色）：选中行霓虹辉光呼吸(2.2s) + 预览面板扫描线(scan) + 高饱和强调色仅用于焦点。
- 所有动效均通过 CSS 变量按 data-theme 切换；含 `@media (prefers-reduced-motion: reduce)` 降级（关闭 loop 动画，仅保留颜色过渡），符合无障碍。
- 颜色仍严格映射到 AppTheme 字段，调色板实时跟随。本任务为设计稿，**未改 C# 代码**，待用户选定后落地；落地时流动渐变/霓虹辉光需 WinForms Timer 自定义绘制（成本高），而按钮 hover 提亮/行淡入/状态脉冲可行。

## 主窗口动态动效主题（第四版 · 精装/非常漂亮，apple-design）
- 用户再次要求"设计几款**非常漂亮**的动态动效主题"。在第三版基础上做**精装升级**：更高保真 Mockup + 更精致动效。新增文件 `主窗口动态动效主题_精装版.html`（区别于第三版 `主窗口动态动效主题_可视化设计.html`）。
- 升级点：①Mockup 提升至接近真实（搜索框、内联 SVG 图标工具栏、侧栏**滑动指示条**(navind transform 过渡)、频道 logo 圆角块、延迟微型条、状态 pill 带图标、预览窗**均衡器跳动 + 进度环 SVG stroke-dashoffset 描边动画**、状态栏进度环）；②动效用 spring 式缓动 `cubic-bezier(.2,.9,.25,1)` 替代纯线性；③新增 **JS 水波纹 ripple**（pointerdown 即触发，真正"响应按压"）；④4 款主题：星河 Aurora(极光流光+辉光呼吸)、蜜桃薄暮 Peach(暖色网格缓动+柔光晕)、液态水银 Mercury(毛玻璃+扫光+水波纹)、霓虹梦境 Neon(霓虹辉光+扫描线)；⑤保留 `@media (prefers-reduced-motion)` 降级。
- 修了一个 CSS typo：`mercury` 主题 `--link:#3a4 fd9` → `#3a4fd9`。
- 仍为设计稿，**未改 C# 代码**，待用户选定后落地（落地现实同第三版说明）。沙箱 Linux 仅出 HTML 稿，动效需在浏览器查看。

## 主窗口玻璃拟态主题（第五版 · 用户选了"玻璃拟态"方向）
- 用户对第四版反馈"不够漂亮"。经 AskUserQuestion 澄清"漂亮"无锚点，用户选定**玻璃拟态(Glassmorphism)**方向。遂放弃泛出多款，聚焦做 3 款高保真玻璃拟态动态动效主题。
- 新增文件 `主窗口玻璃拟态主题_可视化设计.html`。核心做法：背景流动光斑层(4 个 radial-gradient blob 缓慢游动) + 半透明毛玻璃面板(`backdrop-filter:blur(30px) saturate(190%)` + 顶边 1px 高光 inset + 柔和多层投影) ⇒ 玻璃透出动态内容形成**景深**。3 款：极光玻璃 Aurora(深蓝绿紫)、晨曦玻璃 Dawn(浅粉蓝雾)、霓光玻璃 Neon(深品红青亮描边)。
- 保留交互：点击卡片/标题栏三角切换主题、导航滑块 transform 过渡、行 hover 位移、选中行辉光呼吸、`eq` 均衡器跳动、进度环 stroke 描边、播放键呼吸光晕、JS 按压水波纹、动效开关(nofx)、底部 19 字段调色板实时跟随。
- **诚实落地边界（重要变量）**：WinForms 控件本身不透明，GDI+ 无真背景模糊透传；真正的毛玻璃需 DWM `SetWindowCompositionAttribute`(Acrylic/Blur behind) 且只能作用于窗体/特定面板，**数据网格行的"真透"很难实现**。务实落地 = 用 `ApplyWindowShape` 现状 + 半透明 Panel 模拟 + DWM 模糊做窗体级毛玻璃，网格行用"伪玻璃"配色近似；流动光斑需 Timer 驱动单一背景层重绘（绝不能每帧遍历网格设色）。仍为设计稿，待选定落地。

## 主题库外置化（JSON 可加载 + 缺失回退内置）
- 用户需求：把主题封装成库文件，程序启动时加载外部主题；若不含库文件则启用内置主题。已实施（代码正确，但被 Main.cs 预存错误挡住整体编译，见下方障碍）。
- 实现（`AppTheme.cs` 新增，零依赖手写解析）：
  - `LoadExternalThemes()`：从 `Path.Combine(Assembly.GetExecutingAssembly().Location 同级, "themes")` 读取 `*.json`；目录不存在/文件损坏均 `try/catch` 静默回退内置。
  - `ParseThemeJson(string)`：正则 `"key":"value"` 提取，用反射按属性名匹配 `AppTheme` 的 26 字段；颜色值 `#RRGGBB` 经 `ColorTranslator.FromHtml` 解析；忽略未知字段，单字段失败不影响其它。
  - `ExternalThemes`(只读字典) + `IsExternalThemesLoaded()` + `BuiltinThemeNames`(跳过与内置同名的外部主题)。
  - 需加 `using System; System.Collections.Generic; System.IO; System.Linq; System.Reflection; System.Text.RegularExpressions;`（`StringComparer/StringComparison/AppDomain` 在 `System`）。
- 集成（`IPTVLiveCheckerMain.cs`）：
  - `MakeEffectiveTheme()`(L16095)：开头懒加载 `LoadExternalThemes()`；先查 `ExternalThemes[themePreference]`，命中则用之，否则走原内置 switch；外部主题同样走 `Clone()` + HighContrast。
  - 主题菜单(L18084)：6 内置项后加 `ToolStripSeparator` + 遍历 `ExternalThemes` 追加项，点击设 `themePreference` 并 `SetTheme`。
- 示例库 + csproj：`themes/极光玻璃.json`、`themes/霓光梦境.json`（各含全部 26 字段）。csproj 加 `Content Include themes/*.json`(CopyToOutputDirectory=PreserveNewest) + `CopyThemesToOutput` Target 复制到 OutDir/themes。**注意 csproj 真实路径是 `IPTV1.0/IPTVLiveChecker.csproj`（根目录），非 `IPTV1.0/IPTVLiveChecker/` 子目录；themes 必须放 `IPTV1.0/themes/`（csproj 同级），否则 Copy 报 MSB3030**。build 须 `cd IPTV1.0 && dotnet build IPTVLiveChecker.csproj`（用相对路径，绝对 `/c/...` 路径会被 MSBuild 当开关报错 MSB1001）。
- **障碍（非本任务引入，已诊断）**：当前沙箱 `Main.cs` 全量编译报 612 错误 = CS0103 缺失 `At`(516) + `ctx`(96)。`At` 定义仍在 L6194/L11797（`Point At(int x,int y)=>new Point(x-ox,y)`，局部函数，依赖局部 `ox`），`CenterEmptyState`(L21580) 等独立方法调用 `At` 导致脱离作用域——典型某大方法括号破坏/代码被挪到独立方法使局部 `At/ctx` 脱域，属外部/本机最近编辑引入。之前 build 因增量未重编 Main.cs 掩盖；移动 themes 触发全量重建才暴露。本任务的 AppTheme.cs 加载器、MakeEffectiveTheme(L16095)、菜单集成(L18084) 均语法正确、集成完整。需恢复 Main.cs（本机覆盖或 git）后整体编译即通过，主题库功能即生效。
- 结论（已更新）：主题库功能代码已完成且正确。经本次 `dotnet build -p:OutputPath=bin/Verify/` 验证，当前磁盘上 Main.cs 实际**完好、0 错误 0 警告**（先前报告的 612 CS0103 属沙箱瞬时/外部编辑残留，现已不存）。主题库功能已生效。

## 主题动效 + 毛玻璃消失修复（当前任务）
- **根因**：外部主题 JSON + AppTheme 仅含 26 个平涂颜色，无"效果"元数据；主窗 `ApplyTheme()` 只平涂 `BackColor/ForeColor`，**无背景动画层、无 DWM 毛玻璃**；"动态极光/玻璃"此前只存在于 HTML 设计稿，从未落地运行时代码。`NeonPalette.Create` 对暗色主题硬编码青/品红且只用于 `NeonDialog` 弹窗，主窗不用也不动。加载"极光玻璃/霓光梦境"只得到静态纯色。
- **修复（已实施，`dotnet build` 0 错误 0 警告验证）**：
  1. `AppTheme.cs` 新增效果字段 `GlassEnabled/GlassOpacity/GlassBlur/AnimationType/AnimationSpeed/GradientStops`；`Clone()` 同步复制；`ParseThemeJson` 扩展解析 bool/int/double/颜色数组(`GradientStops`)。
  2. 新建 `ThemeFx.cs`：`ThemeFxBackground`(DoubleBufferedPanel+Timer) 按 `AnimationType` 实时绘制 aurora/neon/pulse/fluid/breath 流动光斑，尊重 `AnimationSettings.ReduceMotion`；`ThemeFx.ApplyThemeFx(Form,AppTheme)` 管理最底层背景层与可选 DWM `SetWindowCompositionAttribute(ACCENT_ENABLE_BLURBEHIND, attr 19)`；`ThemeFx.Glass(color,alpha)` 生成半透明玻璃色；句柄未建时延后到 `HandleCreated` 应用。
  3. `ApplyTheme()` 末尾新增 `ApplyGlassFx()` + `ThemeFx.ApplyThemeFx(this, theme)`：玻璃/动效主题下把 titleBarPanel/mainArea/actionArea/searchPanelRef/toolbarRef/statusBar*/bottomBarRef/outerWrap 改为半透明(`GlassOpacity`)，让背后流动极光透出=毛玻璃；非沉浸式恢复不透明（切回内置主题正常）。
  4. `themes/极光玻璃.json`(aurora) 与 `themes/霓光梦境.json`(neon) 补效果元数据。
- **验证/注意**：`bin/Verify` 构建 0 错误；Debug 构建仅因运行中的 exe 被锁(`IPTV直播源检测工具` 进程)导致复制失败，非代码错。用户须**关闭程序→在 VS 重新生成→重启**才能看到效果（运行中的旧 exe 仍是改造前版本）。内置 6 主题未启效果字段、保持原样，向后兼容。落地现实与第五版玻璃拟态边界一致：数据网格行不真透，铬面板半透明+流动背景层近似毛玻璃。

## 主题库打包：所有设计稿主题落地为 JSON（当前任务）
- 用户要求"将之前所有主题打包进来"。将 HTML 设计稿里的主题全部转为可加载的 `themes/*.json`。
- 配色来源：读 5 个设计稿 HTML 的 CSS 变量块（主窗口主题_可视化设计 / 动态主题_可视化设计 / 动态动效主题_可视化设计 / 动态动效主题_精装版 / 玻璃拟态主题_可视化设计），提取每款主题的 bg/bgalt/surface/border/text/accent/play/copy/link/success/error/warn/info 等，补全 26 字段 + 效果元数据（GlassEnabled/GlassOpacity/GlassBlur/AnimationType/AnimationSpeed/GradientStops）。
- 最终 14 个外部主题（含原 2 个）：静态 4（石墨灰/晴空蓝/绛紫/暖砂，GlassEnabled=false 无动画）；动态 10（极光流光 aurora / 霓虹脉冲 neon / 流体玻璃 fluid / 星河 aurora / 液态水银 fluid / 蜜桃薄暮 fluid / 呼吸脉冲 breath / 晨曦玻璃 aurora / 极光玻璃 aurora[原] / 霓光梦境 neon[原]）。
- 用临时 `gen_themes.py` 批量生成并校验（26 字段齐全、hex 合法、动效主题 GradientStops≥2，0 BAD），已删除脚本。`dotnet build -p:OutputPath=bin/Verify/` 0 错误，14 个 JSON 已复制到 `bin/Verify/themes/`。主题菜单 L18236-18241 已确认会 `LoadExternalThemes()` 并逐个 `Items.Add(ex.Name)`，全部出现在主题下拉中。
- 注意：`Name` 不可与内置 6 主题同名（否则被 `BuiltinThemeNames` 跳过）；现 14 个名称均唯一。

## 主题动效遮挡标题栏图标/按钮修复（当前任务补充）
- **现象**：加载"极光玻璃/霓光梦境"后，左上角动态特效遮挡软件图标与按钮。
- **根因**：`ApplyGlassFx()` 沉浸式分支把 `titleBarPanel`(左上角标题图标+文件/检测/搜索/设置/关于导航按钮所在)与 `toolbarRef` 也设成半透明(`ThemeFx.Glass(theme.Bg/BgAlt, a)`，`a`=`GlassOpacity` 默认 205)，背后流动极光透显到图标按钮背后造成"被遮挡"观感。`ThemeFxBackground` 本身在最底层(`SetChildIndex 0`)并未压住控件——是半透明铬面板让亮色光斑透出。
- **修复（v2 毛玻璃式）**：用户进一步要求"将动态特效放置在背景"。改为：标题栏/工具栏/搜索区用**磨砂薄层** `ThemeFx.Glass(theme.Bg/BgAlt, chromeA)`，`chromeA = clamp(GlassOpacity+28 → [225,248])`（默认≈233，比初版 205 更实），让极光作为**柔和背景**透出、图标按钮清晰（毛玻璃质感，不遮挡）；正文/状态栏仍按 `GlassOpacity`(默认205) 明显透出极光。即"流动极光=窗口动画背景，标题栏磨砂、正文玻璃"的玻璃拟态分层。`dotnet build -p:OutputPath=bin/Verify/` 0 错误 0 警告验证。

## 动态光影"总在左上角遮挡组件"根因定位与彻底修复（当前任务 · 关键）
- **用户问题**：所有动态主题加载后，动态光影总在左上角显示并遮挡组件；要求"优化修复 + 确认软件是否支持动态主题显示"。
- **确认：软件支持动态主题显示**。`ThemeFx.cs` 的 `ThemeFxBackground`(DoubleBufferedPanel+Timer) 按 `AnimationType` 实时绘制极光/霓虹/脉动/流体/呼吸光斑，作为主窗 `form.Controls` 最底层(index 0, `DockStyle.Fill`)背景；`ApplyThemeFx` 在 `ApplyTheme` 中调用；14 个外部主题 JSON 均带 `AnimationType/GlassEnabled`。编译/机制正常（非背景层位置问题）。
- **真正的遮挡根因（非背景层，而是透明度串色到交互控件）**：
  - 标题栏 5 个导航按钮 `btnNavFile/btnNavDetect/btnNavSearch/btnNavSettings/btnNavAbout` 创建时 `BackColor = Color.Transparent`，且其 `PaintNavButton`(委托 L18464) 用 **半透明的** `(btn.Parent != null) ? btn.Parent.BackColor : Color.White` 填充按钮背景——而 `Parent` 即被 `ApplyGlassFx` 设为半透明的 `titleBarPanel`，于是背后流动的极光 100% 透显到按钮上（图标+按钮恰在**左侧/左上角**）→ 表现"左上角动态光影遮挡组件"。
  - `titleIcon`(PictureBox) 创建时 `BackColor = Color.Transparent` → 图标框也透出极光。
  - 右侧 `btnThemeToggle/btnMin/btnMax/btnClose`（`CreateTitleButton`）`BackColor = theme.Bg`(不透明) + 自定义 Paint 只画图标、不填充背景 → 正常，不遮挡。
  - 工具栏/状态栏按钮（`btnStartDetect` 等）均为不透明 `BackColor` + `StyleRoundButton` → 正常。
- **修复（玻璃拟态标准做法：背景动效保留，交互控件改不透明）**：
  1. `PaintNavButton` 的 clearBrush 改为不透明实色 `Color.FromArgb(255, theme.Bg.R, theme.Bg.G, theme.Bg.B)`（即 opaque theme.Bg），导航按钮不再透出极光。
  2. `titleIcon` 创建 `BackColor` 由 `Color.Transparent` 改为 `theme.Bg`；并在 `ApplyGlassFx` 开头强制 `titleIconRef.BackColor = theme.Bg`（覆盖所有主题/切换，保持不透明）。
  3. 标题栏面板仍保持磨砂薄层(chromeA≈225-248)作为柔和环境光，但按钮/图标不透明 → 极光可见为标题栏环境光、绝不透到控件上 = 不遮挡。
- **验证**：`dotnet build IPTVLiveChecker.csproj -p:OutputPath=bin/Verify/`（注：当前目录已 `cd IPTV1.0`）→ **0 错误 0 警告**。改的是共享 `PaintNavButton`/`titleIcon`，14 个动态主题全部受益。
- **调优提示（告知用户）**：若想极光在标题栏更明显，调低主题 JSON 的 `GlassOpacity`(如 205→160 极光更透出，但控件已不透明故安全)；若想标题栏完全实色，把 `GlassOpacity` 调到 248 或改 `ApplyGlassFx` 让 `titleBarPanel` 走不透明分支即可。运行时仍需本机 Windows 确认视觉。

## 动态主题"整窗光晕覆盖所有组件"根治（v3 卡片式玻璃拟态）
- **用户问题**：上一轮毛玻璃式仍导致"极光光晕覆盖了整个窗口界面、所有组件控件被覆盖"。
- **根因（两层）**：
  1. `ApplyGlassFx` 沉浸式分支把铺满全窗的 `outerWrap`/`mainArea`/`actionArea`/`statusBarContainer` 全部设成半透明玻璃(`ThemeFx.Glass(..., a)`，a≈205)，半透明面板叠加让底层 `ThemeFxBackground` 极光大面积透出、糊住整窗。
  2. 动态主题 `GlassBlur:true` 触发 DWM `ACCENT_ENABLE_BLURBEHIND` 窗口级模糊——让整个窗口(含控件)半透明并显示模糊**桌面**，极光被桌面取代，造成"整窗被覆盖"观感。
- **修复（v3 卡片式玻璃拟态 + 关 DWM 模糊）**：
  - `ApplyGlassFx` 沉浸式分支改为：极光作为整窗动画背景(最底层)，`outerWrap`/`mainArea`/`actionArea` 设 `Color.Transparent` 让极光透出；承载组件的"卡片"（`titleBarPanel`=theme.Bg、`toolbarRef`/`searchPanelRef`=theme.BgAlt、`gridContainerRef`=theme.BgAlt 数据卡片、`statusBarContainer`/`bottomBarRef`=theme.Bg）**保持不透明、清晰浮在极光上**；`mainArea.Padding=8` 让数据卡片浮起、四周露出流动极光；非沉浸式分支重置 `mainArea.Padding=0` 并恢复不透明实色（向后兼容）。
  - 批量将 10 个动态/玻璃主题 `GlassBlur` 由 `true`→`false`，毛玻璃纯靠"透明外壳+流动极光+不透明卡片"实现，不再依赖 DWM 桌面模糊。
- **验证**：`dotnet build -p:OutputPath=bin/Verify2/` → 0 错误 0 警告。14 个主题 JSON 已 cp 到 `bin/Debug/net472/themes` 与 `bin/Verify2/themes`。
- **效果**：整窗流动极光作为背景，标题栏/工具栏/数据网格/状态栏/左侧按钮均为不透明卡片浮于其上，组件 100% 清晰、绝不被光晕覆盖。用户须**关闭程序→VS 重新生成→重启**后切到任意动态主题查看。
